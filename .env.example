# Gemini powers live AI quest/chat/vision/image flows.
# Leave blank for fallback demo mode.
GEMINI_API_KEY=

# Public deployed app URL, used for self-referential links and callbacks when needed.
APP_URL=http://localhost:3000

# Render/Railway/Fly inject PORT automatically in production. Local default is 3000.
PORT=3000
